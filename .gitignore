# macOS / FUSE noise
.DS_Store
.fuse_hidden*

# Respaldos y salidas de análisis (solo local)
_backup-2026-04-21/
graphify-out/

# Node
node_modules/

# Internos / estrategia — solo local, NO publicar en Cloudflare Pages
# OJO: Cloudflare Pages sirve el repo TAL CUAL. Todo lo trackeado queda
# público en origenlab.com/<ruta>. Verificado 2026-07-15: CLAUDE.md y
# _docs/46 estaban accesibles con HTTP 200 desde el 2026-06-04.
_borradores-offpage-*.md
_AUDITORIA/
MEJORA-ORIGENLAB/
_branding/
_reports/
MASTER WEB PRODUCTION SYSTEM/
Estudio_de_Mercado_*.docx
automate_whatsapp.html
_docs/
CLAUDE.md
AGENTS.md
CONSULTORIO-WEB/
*.plugin
_REPORTE_HEADERS.txt
ANALISIS-*.md
*.xlsx
*.docx
*.csv

# Scripts de desarrollo / deploy — no son contenido del sitio
_add_paquetes_nav.py
_finish-git-setup.sh
_push-github.sh
_push-origenlab-com.sh
_serve.sh
_cleanup-huerfanos.sh

# Herramientas de deploy / secretos — NO publicar
_tools/

# Locks y temporales de Office
.~lock.*#
~$*
_PROMPT-GITHUB-CHROME.md

# Cache local de wrangler (contiene datos de cuenta Cloudflare) — NO publicar
.wrangler/

# ── Migración a Astro 6 — vive en la rama `canonical-astro6-migration` ──
# NUNCA en main. Cloudflare Pages sirve main TAL CUAL desde la raíz: cada
# archivo trackeado queda público. El 2026-07-17 `dist/` se coló en main y
# publicó 173 páginas duplicadas (origenlab.com/dist/blog/...) e indexables,
# más los .md fuente en /src/. Verificado con HTTP 200 en producción.
dist/
src/
_migration/
package-lock.json
package.json
astro.config.mjs
tsconfig.json
.astro/
# --- OrigenLab: documentacion fuera del repo (vault OBSIDIAN) ---
# Regla: el repo del sitio solo lleva archivos del sitio. Auditorias, estudios,
# planes y notas viven en Projects/OBSIDIAN/. Nada de esto llega al dist/ igual,
# solo engorda el repo. Ver CLAUDE.md.
/docs/
/research/
/investigacion/
/graphify-out/
/obsidian-vault/
/_docs/
/AUDITORIA*
/AUDITORÍA*
/ANALISIS*
/ANÁLISIS*
/ESTUDIO*
/INFORME*
/REPORTE*
/RESUMEN*
/DIAGNOSTICO*
/PROPUESTA*
/BLUEPRINT*
/CHECKLIST*
/CHANGELOG-SEO*
/CHANGELOG-AEO*
/PLAN-*
/PLAN_*
/FASE-*
/FASE_*
/PROMPT-*
/PROMPT_*
/HANDOFF*
/RUNBOOK*
/BITACORA*
/SESION*
/STATUS_*
/*.docx
/*.xlsx
/*.pptx
# worktrees: copias de trabajo, nunca se versionan
.worktrees/
.claude/worktrees/
